services:
  langgraph-postgres:
    image: postgres:17.4
    container_name: capstone-langgraph-db        # ← nice name in Docker Desktop
    environment:
      POSTGRES_USER: ${LANGGRAPH_POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${LANGGRAPH_POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${LANGGRAPH_POSTGRES_DB:-langgraph_memory}
    ports:
      - "${LANGGRAPH_POSTGRES_PORT:-5433}:5432"
    volumes:
      - langgraph_postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test:
        - "CMD-SHELL"
        - "pg_isready -U ${LANGGRAPH_POSTGRES_USER:-postgres} -d ${LANGGRAPH_POSTGRES_DB:-langgraph_memory}"
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  langgraph_postgres_data:
    name: capstone-langgraph-data                  # ← named volume = easier to inspect/delete